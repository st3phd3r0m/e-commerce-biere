{% extends 'base.html.twig' %}

{% block title %}BierShop - Résultat de votre recherche{% endblock %}

{% block stylesheets %}
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/14.6.0/nouislider.min.css">
{% endblock %}

{% block body %}
<div class="container">

    {% if expr is defined %}
        <h1 class="mb-5">{{ numberOfResults }} résultat{{ (numberOfResults>1)? 's' : '' }} pour l'expression <em>"{{ expr }}"</em></h1>
    {% endif %}  


        <section>     

                <div class="row py-0">
                    {{ knp_pagination_render(products) }}
                </div>  

                <div class="row">
                    {% set col = 3 %}

                    {% for product in products %} 
                        {{ include('home/_listProducts.html.twig') }}
                    {% endfor %}
                </div>

                <div class="row py-0">
                    {{ knp_pagination_render(products) }}
                </div>    
        </section>
   
</div>    
{% endblock %}
{% block javascripts %}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/14.6.0/nouislider.min.js"></script>
    {{ encore_entry_script_tags('sliderFilter') }}
{% endblock %}