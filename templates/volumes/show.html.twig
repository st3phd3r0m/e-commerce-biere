{% extends 'base.html.twig' %}

{% block title %}Volumes{% endblock %}

{% block body %}
    <h1>Volumes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ volume.id }}</td>
            </tr>
            <tr>
                <th>Volume</th>
                <td>{{ volume.volume }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('volumes_index') }}">back to list</a>

    <a href="{{ path('volumes_edit', {'id': volume.id}) }}">edit</a>

    {{ include('volumes/_delete_form.html.twig') }}
{% endblock %}
