{% extends 'base-admin.html.twig' %}

{% block title %}Edition du panier{% endblock %}

{% block body %}
<!-- Début du main -->
<main id="layoutSidenav_content">
    <div class="container-fluid">

        <ul class="breadcrumb mb-4">
            <li class="breadcrumb-item"><a href="{{ path('dashboard') }}">Dashboard</a></li>
            <li class="breadcrumb-item active">Paniers</li>
        </ul>
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-cart-plus"></i></i>
                Modification du panier
            </div>
            <div class="card-body">

                {{ include('cart/_form.html.twig', {'button_label': 'Valider'}) }}

                <a class="btn btn-primary" role="button" href="{{ path('cart_index') }}">Retour à la liste panier</a>
            </div>    
        </div>
    </div>
</main>

</div>

{% endblock %}
