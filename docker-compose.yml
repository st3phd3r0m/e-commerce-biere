version: "3.3"

services:
  php-fpm:
    build: ./
    ports:
      - 5000:8000
    volumes:
      - ./:/var/www/html
      - ./config/php.ini-production:/usr/local/etc/php/php.ini
  database:
    image: postgres:13-alpine
    restart: always
    environment:
      ***REMOVED***
      ***REMOVED***
      ***REMOVED***
    ports: [5432]

  pgadmin:
    depends_on:
      - database
    image: dpage/pgadmin4
    restart: always
    environment:
      ***REMOVED***
      ***REMOVED***
    ports:
      - "5050:80"
      